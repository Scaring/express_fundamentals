const { tours } = require('../helpers');

const getAllTours = (req, res) => {
  res.status(200).json({
    status: 'success',
    results: tours.length,
    data: { tours },
  });
};

module.exports = getAllTours;
